/**
 * @file Public API for the home feature.
 * Only exports the top-level view — internal components
 * (ExerciseForm, SessionResult, ChevronIcon, useExerciseForm)
 * are implementation details consumed only within this feature.
 */
export { HomeView } from './HomeView'
