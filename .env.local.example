# CalisthenIQ Environment Variables

# MSW: set "true" to use in-browser mocks (no backend needed)
NEXT_PUBLIC_MSW_ENABLED=false

# API base URL (leave empty for relative paths via netlify dev)
NEXT_PUBLIC_API_BASE_URL=http://localhost:9999

# API timeout in milliseconds (default: 10000)
# NEXT_PUBLIC_API_TIMEOUT=10000
